# Handoff — 2026-07-09 sync #906/#907 merges + triage new issues #908/#909

## Task

Docs-only sync: record that PR #906 (`profile_repo.py` connection-leak fix) and PR #907
(#758 job-key unification) are merged and live, then triage two new issues opened after the
last board-health scan (`HANDOFFS/2026-07-09-board-health-scan.md`, 34 issues) — #908 (bug) and
#909 (governance doc request) — so the board stays current.

## Context

- Repository: Binz2008-star/Rico-Your-AI-intelligent-job-hunt-partner-in-the-UAE
- Branch: `docs/908-909-triage-and-906-907-sync` (docs-only)
- Issue or PR: #906 (merged), #907 (merged), #908 (new, triage only), #909 (new, triage only)
- Relevant files: none changed by this PR besides `AI_WORKSPACE/*`
- Relevant architecture notes: `AI_WORKSPACE/HANDOFFS/2026-07-09-board-health-scan.md` (last full
  scan, 34 issues, did not include #908/#909 — both opened afterward)

## Constraints

- Out of scope: any code change, test change, label change, issue close, Neon/Vercel/Render touch
- This PR only documents state; it does not implement #908 or #909

## #906 / #907 — merged and live

- **#906** (`fix(profile_repo): close leaked Neon connections on 8 call sites`) — merged
  `f3eb122bf9cc0867f2e1dd64a37234a009296cc7`. Vercel production confirmed READY for this SHA
  (alias `ricohunt.com`). Fixed 8 leaking `with db.connect() as conn:` sites in
  `src/repositories/profile_repo.py` (the original 2026-07-09 deep-dive estimate of 5 was a
  truncated grep; the full-file read found 8). Closes the "profile_repo.py DB connection leak"
  risk carried in `PROJECT_STATUS.md` since the security/data-risk deep dive.
- **#907** (`fix(save-key): unify job-key scheme between auto-persist and explicit chat save`) —
  merged `ec06ef54d45f6ba81d7ee764e55a1bf8ffa94081`, closes #758. `_fallback_identity`
  (`src/services/job_save.py`) now delegates to `src.applications.get_job_id()`, matching
  `RicoChatAPI._derive_lifecycle_job_key`, so both save paths key the same job identically and
  `ON CONFLICT (user_id, job_key)` fires instead of leaving an orphaned duplicate row. Production
  deployment verification for this SHA is tracked separately (see next session/handoff).

`main` is now at `ec06ef5` (`origin/main`).

## New issue #908 — BUG: Attachment-first reasoning is bypassed

- Opened 2026-07-09, `bug` label, owner-flagged **Priority: High**.
- Observed: uploaded screenshot (Mashreq rejection email) misclassified at 3% confidence as
  "Application Confirmation"; first response was generic instead of an attachment summary;
  follow-up question about the attachment triggered profile-context injection and an unrelated
  ESG/environmental job-search suggestion; an unrelated invoice (`Invoice-W7JSBCPT-0003.pdf`) was
  referenced as the "Active CV".
- **Owner's own follow-up comments (2 total) explicitly reframe this as an orchestration/intent-
  routing problem, not a document-classification bug**: "Treat this as an orchestration/intent-
  routing issue rather than only a document-classification issue" and "Do not create separate
  fixes for each symptom... inspect the conversation orchestration pipeline, intent routing
  precedence, attachment processing order, tool-selection policy, active-document/CV selection
  logic. Only then patch the minimal root cause(s)."
- **This is explicitly NOT a small runtime patch** (unlike #906/#907) — the owner is asking for a
  root-cause investigation across the chat orchestration/intent-routing pipeline
  (`src/rico_chat_api.py`, CV/attachment classification, tool-selection policy, active-CV
  selection) before any fix is written. Per this repo's cost-optimization rule
  (`CLAUDE.md`), a multi-file orchestration deep-dive needs owner sign-off on scope/cost before
  it is launched — this handoff records the triage only; the investigation itself is not started.

## New issue #909 — GOVERNANCE: Add Rico Product Constitution and Development Quality Gate

- Opened 2026-07-09, `documentation` label, owner-flagged **Priority: High**.
- Asks for `AI_WORKSPACE/GOVERNANCE/RICO_CONSTITUTION.md`,
  `AI_WORKSPACE/GOVERNANCE/RICO_ENGINEERING_PRINCIPLES.md`,
  `AI_WORKSPACE/GOVERNANCE/RICO_DEVELOPMENT_QUALITY_GATE.md`, and an `AI_WORKSPACE/GOVERNANCE/ADR/`
  folder.
- **Flagging a direct conflict before any doc is written**, per `CLAUDE.md`'s "stop and report the
  conflict instead of guessing" rule: this repo already carries functionally equivalent, Active
  documents —
  - `AI_WORKSPACE/RICO_EXECUTION_PRINCIPLES.md` — listed in `MASTER_INDEX.md` as "Product
    constitution (v2.0)".
  - `AI_WORKSPACE/AGENT_OPERATING_MODEL.md` — the accepted agent operating model.
  - `AI_WORKSPACE/PR_QUALITY_GATE_RULES.md` / `AI_WORKSPACE/PR_CHECKLIST.md` — the existing
    agent-side PR quality gate + checklist.
  - `AI_WORKSPACE/DECISIONS.md` — already serves as the ADR log (see MASTER_INDEX's "ADR index"
    section, which explicitly states "no parallel ADR system").
  - This repo has **already rejected a duplicate `GOVERNANCE/` folder once**: PR #901's history
    shows a commit `docs: drop parallel GOVERNANCE files from PR #901` with the reasoning
    "`AI_WORKSPACE/AGENT_OPERATING_MODEL.md` (merged in #890) is already the accepted agent
    operating model... The GOVERNANCE/ files duplicated it, introduced an unreferenced 'Devin'
    role, and archived a v1.0 constitution that never existed in this repo's history."
- Recommendation (not yet actioned — owner decision needed): either (a) close #909 as
  substantially satisfied by the existing docs and fold any genuinely new content (the attachment-
  first conversation-hierarchy / source-hierarchy / tool-safety principles, which are new and
  useful, and read directly on point for #908) into the existing `RICO_EXECUTION_PRINCIPLES.md` /
  `AGENT_OPERATING_MODEL.md` / `PR_QUALITY_GATE_RULES.md` files, or (b) if the owner wants a
  dedicated `GOVERNANCE/` namespace despite the prior rejection, say so explicitly so this isn't
  re-litigated a third time.

## Continuity Block

- Task ID: TASK-20260709-004
- GitHub issue/PR: #906 (merged), #907 (merged), #908 (triage only), #909 (triage only)
- Branch: `docs/908-909-triage-and-906-907-sync`
- Base branch: main
- Last safe commit SHA: ec06ef54d45f6ba81d7ee764e55a1bf8ffa94081
- Current head SHA: (this docs commit, no runtime change)
- Status: done (triage/sync only; #908 investigation and #909 decision both await owner direction)
- Files changed: `AI_WORKSPACE/PROJECT_STATUS.md`, `AI_WORKSPACE/TASKS.md`, this handoff,
  `AI_WORKSPACE/MASTER_INDEX.md`
- Files intentionally not touched: all runtime code, tests, Neon, Vercel/Render config, issue
  labels/state, any `GOVERNANCE/` files (per the #909 conflict flagged above)
- Known blockers: owner decision needed on (a) #908 orchestration deep-dive scope/cost approval,
  (b) #909 — reuse existing governance docs vs. create a parallel `GOVERNANCE/` folder
- Validation already run: `git log origin/main` (confirms `ec06ef5` HEAD, #906+#907 both present);
  `list_issues`/`issue_read` cross-check (34 → 36 open issues; #908/#909 confirmed new since the
  2026-07-09 board-health scan)
- Validation still required: none for this docs-only sync
- Next exact action: owner decides #908/#909 priority and #909 doc-reuse-vs-new question; #812
  (compound-title role splitting) proceeds separately per prior explicit approval
- Stop condition: do not start #908's orchestration investigation or write any `GOVERNANCE/` file
  for #909 without explicit owner direction
- Rollback plan: revert this docs-only PR; no schema/env/runtime changes

## Deliverables

- Changed files: `AI_WORKSPACE/PROJECT_STATUS.md`, `AI_WORKSPACE/TASKS.md`, this handoff,
  `AI_WORKSPACE/MASTER_INDEX.md`
- Implementation summary: docs-only sync of #906/#907 merge state + triage of #908/#909
- Tests run: none (docs-only)
- Risks: none — pure documentation, no schema/env/runtime changes
- Rollback notes: revert this PR; no schema/env/runtime changes
