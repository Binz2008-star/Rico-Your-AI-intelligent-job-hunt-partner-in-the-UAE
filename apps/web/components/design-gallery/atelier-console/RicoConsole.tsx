"use client";

/**
 * Atelier Console barrel — re-exports the extracted chat core.
 *
 * `RicoChat` now lives in `./RicoChat.tsx`; this file remains so existing
 * consumers (e.g. `./index.tsx`) can keep importing from `./RicoConsole`.
 */

export { RicoChat } from "./RicoChat";

